import BaseUseRouter from '../pages/baseUseRouter';

const routes = [
    {
        path: '/baseUse',
        component: BaseUseRouter,
        exact: true,
    },
];

export default routes;
